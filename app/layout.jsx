export const metadata = {
  title: "48RENT Camera Rental",
  description: "Camera Rental Calculator",
};

export default function RootLayout({ children }) {
  return (
    <html lang="th">
      <body>
        {children}
      </body>
    </html>
  );
}
